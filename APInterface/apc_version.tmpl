/* 
 * This file is automatically generated by the build process
 * DO NOT EDIT!
 */

#include "apc_build.h"
#include <string.h>
#include <stdio.h>

// The following VER_ lines must be left intact. 
// These lines read in the release process to extract the version.

const int VER_MAJOR = 1;
const int VER_MINOR = 0;
const int VER_PATCH = 1;

// The following functions are declared in the common Version.h header

int getMajorVersion() { return VER_MAJOR; }
int getMinorVersion() { return VER_MINOR; }
int getReleaseVersion() { return VER_PATCH; }
int getBuildVersion() { return VER_BUILD; }

const char* getBuildTimestamp() { return "$VER_TIMESTAMP"; }

const char* getBuildName() {
   // for readability, add a hyphen in front of the build name (if it's not empty)
   return strlen("$VER_BUILD_NAME") > 0 ? "-$VER_BUILD_NAME" : "";
}

const char* getVersionLabel() {
   static char verStr[64];
#ifdef WIN32
   sprintf_s(verStr, 64, "%d.%d.%d.%d%s (built %s)", VER_MAJOR, VER_MINOR, VER_PATCH,
             VER_BUILD, getBuildName(), getBuildTimestamp());
#else
   snprintf(verStr, 64, "%d.%d.%d.%d%s (built %s)", VER_MAJOR, VER_MINOR, VER_PATCH,
            VER_BUILD, getBuildName(), getBuildTimestamp());
#endif
   return verStr;
}
